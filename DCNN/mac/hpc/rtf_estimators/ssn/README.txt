This folder is used to perform experiments comparing the performance of different RTF estimators.

It mainly uses reconstruction loss as an indication of the performance of the RTF currently.

It calls the exp.py script to run the experiments